<style>
  .page-container { padding: 32px; max-width: 1200px; margin: 0 auto; }
  .page-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; }
  .page-title { font-size: 24px; font-weight: 700; color: var(--text-primary); margin: 0; }
  
  .templates-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 24px; }
  
  .template-card { background: var(--bg-surface); border: 1px solid var(--glass-border); border-radius: var(--radius-lg); padding: 24px; transition: transform 0.2s, box-shadow 0.2s; display: flex; flex-direction: column; }
  .template-card:hover { transform: translateY(-2px); box-shadow: var(--shadow-md); border-color: rgba(255,255,255,0.1); }
  
  .tpl-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 16px; }
  .tpl-title { font-size: 16px; font-weight: 600; color: white; margin: 0; }
  .tpl-type { display: inline-flex; align-items: center; gap: 4px; font-size: 11px; font-weight: 600; padding: 4px 8px; border-radius: 20px; text-transform: uppercase; }
  .tpl-type.whatsapp { background: rgba(34, 197, 94, 0.1); color: #22c55e; }
  .tpl-type.email { background: rgba(59, 130, 246, 0.1); color: #60a5fa; }
  
  .tpl-body { font-size: 13px; color: var(--text-secondary); margin-bottom: 24px; flex: 1; line-height: 1.5; background: rgba(0,0,0,0.2); padding: 12px; border-radius: var(--radius-md); font-family: monospace; white-space: pre-wrap; }
  
  .tpl-footer { display: flex; justify-content: flex-end; gap: 8px; padding-top: 16px; border-top: 1px solid var(--glass-border); }

  @media (max-width: 768px) {
    .page-container { padding: 16px; }
    .page-header { flex-direction: column; gap: 12px; align-items: stretch; }
    .page-title { font-size: 20px; }
    .page-header .btn { text-align: center; }
    .templates-grid { gap: 16px; grid-template-columns: 1fr; }
    .template-card { padding: 16px; }
    .tpl-footer { flex-direction: column; }
    .tpl-footer .btn { width: 100%; text-align: center; }
    .modal-container { width: 95%; margin: 20px auto; border-radius: 12px; }
  }

  @media (max-width: 480px) {
    .page-container { padding: 12px; }
    .page-title { font-size: 18px; }
    .templates-grid { gap: 12px; }
    .template-card { padding: 12px; }
    .tpl-header { flex-direction: column; gap: 8px; }
    .tpl-body { font-size: 12px; padding: 8px; }
    .modal-overlay { align-items: flex-end; }
    .modal-container { width: 100%; margin: 0; border-radius: 16px 16px 0 0; max-height: 90vh; overflow-y: auto; }
  }
</style>

<div class="page-container">
  <div class="page-header">
    <h1 class="page-title">Message Templates</h1>
    <button class="btn btn-primary" onclick="openModal('templateModal')">+ New Template</button>
  </div>

  <div style="margin-bottom: 32px;">
    <h2 style="color: white; margin-bottom: 16px; font-size: 20px;">Business Templates</h2>
    <div class="templates-grid">
      {{#each businessTemplates}}
      <div class="template-card">
        <div class="tpl-header">
          <h3 class="tpl-title">{{title}}</h3>
          <span class="tpl-type {{type}}">
            {{#if (eq type 'whatsapp')}}
            <svg width="12" height="12" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51a12.8 12.8 0 0 0-.57-.01c-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 0 1-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 0 1-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 0 1 2.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0 0 12.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 0 0 5.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 0 0-3.48-8.413Z"/></svg> WhatsApp
            {{else}}
            <svg width="12" height="12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg> Email
            {{/if}}
          </span>
        </div>
        <div class="tpl-body" id="tpl-content-{{id}}">{{content}}</div>
        <div class="tpl-footer">
          <button class="btn btn-outline btn-sm" onclick="openEditModal(this)" data-id="{{id}}" data-title="{{title}}" data-type="{{type}}" data-category="{{category}}">Edit</button>
          <button class="btn btn-primary btn-sm" onclick="useTemplate({{id}})">Use Template</button>
        </div>
      </div>
      {{else}}
      <div style="grid-column: 1 / -1; text-align: center; padding: 48px; background: rgba(0,0,0,0.2); border-radius: 12px;">
        <h3 style="margin-bottom: 8px; color: white;">No Business Templates</h3>
        <p style="color: var(--text-secondary);">Create templates to quickly message clients.</p>
      </div>
      {{/each}}
    </div>
  </div>

  <div>
    <h2 style="color: white; margin-bottom: 16px; font-size: 20px;">Personal Templates</h2>
    <div class="templates-grid">
      {{#each personalTemplates}}
      <div class="template-card">
        <div class="tpl-header">
          <h3 class="tpl-title">{{title}}</h3>
          <span class="tpl-type {{type}}">
            {{#if (eq type 'whatsapp')}}
            <svg width="12" height="12" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51a12.8 12.8 0 0 0-.57-.01c-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 0 1-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 0 1-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 0 1 2.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0 0 12.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 0 0 5.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 0 0-3.48-8.413Z"/></svg> WhatsApp
            {{else}}
            <svg width="12" height="12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg> Email
            {{/if}}
          </span>
        </div>
        <div class="tpl-body" id="tpl-content-{{id}}">{{content}}</div>
        <div class="tpl-footer">
          <button class="btn btn-outline btn-sm" onclick="openEditModal(this)" data-id="{{id}}" data-title="{{title}}" data-type="{{type}}" data-category="{{category}}">Edit</button>
          <button class="btn btn-primary btn-sm" onclick="useTemplate({{id}})">Use Template</button>
        </div>
      </div>
      {{else}}
      <div style="grid-column: 1 / -1; text-align: center; padding: 48px; background: rgba(0,0,0,0.2); border-radius: 12px;">
        <h3 style="margin-bottom: 8px; color: white;">No Personal Templates</h3>
        <p style="color: var(--text-secondary);">Create personal assessment templates.</p>
      </div>
      {{/each}}
    </div>
  </div>
</div>

<!-- Template Modal -->
<div class="modal-overlay" id="templateModal">
  <div class="modal-container">
    <div class="modal-header">
      <h2 class="modal-title">Create New Template</h2>
      <button class="modal-close" onclick="closeModal('templateModal')">&times;</button>
    </div>
    <form id="templateForm" onsubmit="submitTemplate(event)" style="padding: 24px; display: flex; flex-direction: column; gap: 16px;">
      <input type="hidden" name="id" id="templateId">
      <div class="form-group">
        <label>Title</label>
        <input type="text" name="title" class="form-control" required placeholder="e.g. Follow-up Email">
      </div>
      <div class="form-group">
        <label>Type</label>
        <select name="type" class="form-control" required>
          <option value="email">Email</option>
          <option value="whatsapp">WhatsApp</option>
        </select>
      </div>
      <div class="form-group">
        <label>Category</label>
        <select name="category" class="form-control" required>
          <option value="BUSINESS">Business Template</option>
          <option value="PERSONAL">Personal Template</option>
        </select>
      </div>
      <div class="form-group">
        <label>Message Content</label>
        <textarea name="content" class="form-control" rows="6" required placeholder="Use {{name}} for client name..."></textarea>
      </div>
      <div class="modal-footer" style="margin-top: 8px; padding: 0;">
        <button type="button" class="btn-secondary" onclick="closeModal('templateModal')">Cancel</button>
        <button type="submit" class="btn-primary">Save Template</button>
      </div>
    </form>
  </div>
</div>

<script>
  function openModal(id) {
    if (id === 'templateModal') {
      document.getElementById('templateForm').reset();
      document.getElementById('templateId').value = '';
      document.querySelector('#templateModal .modal-title').textContent = 'Create New Template';
    }
    document.getElementById(id).style.display = 'flex';
  }
  
  function closeModal(id) {
    document.getElementById(id).style.display = 'none';
  }
  
  function openEditModal(btn) {
    const id = btn.getAttribute('data-id');
    const title = btn.getAttribute('data-title');
    const type = btn.getAttribute('data-type');
    const category = btn.getAttribute('data-category');
    const content = document.getElementById('tpl-content-' + id).textContent;
    
    document.getElementById('templateId').value = id;
    document.querySelector('#templateForm [name="title"]').value = title;
    document.querySelector('#templateForm [name="type"]').value = type;
    document.querySelector('#templateForm [name="category"]').value = category || 'BUSINESS';
    document.querySelector('#templateForm [name="content"]').value = content;
    
    document.querySelector('#templateModal .modal-title').textContent = 'Edit Template';
    document.getElementById('templateModal').style.display = 'flex';
  }
  
  function useTemplate(id) {
    const content = document.getElementById('tpl-content-' + id).textContent;
    navigator.clipboard.writeText(content).then(() => {
      if (typeof showToast === 'function') {
        showToast('Success', 'Template copied to clipboard!');
      } else {
        alert('Template copied to clipboard!');
      }
    }).catch(err => {
      console.error('Failed to copy', err);
      alert('Failed to copy to clipboard');
    });
  }
  
  async function submitTemplate(e) {
    e.preventDefault();
    const form = e.target;
    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());
    const isEdit = !!data.id;
    
    try {
      const btn = form.querySelector('button[type="submit"]');
      btn.disabled = true;
      btn.textContent = 'Saving...';
      
      const url = isEdit ? '/api/crm/templates/' + data.id : '/api/crm/templates';
      const method = isEdit ? 'PUT' : 'POST';
      
      const res = await fetch(url, {
        method: method,
        headers: {
          'Content-Type': 'application/json',
          'Authorization': 'Bearer ' + localStorage.getItem('accessToken')
        },
        body: JSON.stringify(data)
      });
      
      if (res.ok) {
        window.location.reload();
      } else {
        const err = await res.json();
        alert(err.error || 'Failed to save template');
        btn.disabled = false;
        btn.textContent = 'Save Template';
      }
    } catch (error) {
      alert('Error connecting to server');
      const btn = form.querySelector('button[type="submit"]');
      btn.disabled = false;
      btn.textContent = 'Save Template';
    }
  }
</script>
